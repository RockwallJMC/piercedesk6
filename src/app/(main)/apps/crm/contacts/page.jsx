import ContactsList from '@/components/sections/crm/contacts-list';

export const metadata = {
  title: 'Contacts | CRM',
};

export default function ContactsListPage() {
  return <ContactsList />;
}
